;;; win.el --- -*- lexical-binding: t; -*-
;;; Commentary:
;;; Code:

(use-package ace-window)

(use-package winner
  :config
  (winner-mode 1))

(use-package dashboard)

(provide 'win)
;;; win.el ends here
